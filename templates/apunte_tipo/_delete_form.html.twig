<form method="post" action="{{ path('apunte_tipo_delete', {'id': apunte_tipo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apunte_tipo.id) }}">
    <button class="btn">Delete</button>
</form>
