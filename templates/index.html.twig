<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Hola Multimedios{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block body %}
        
            <p>Links a los otros menus</p>
            <ul>
                <li><a href="{{path('contenido_index')}}">Contenido</a></li>
                <li><a href="{{path('usuario_index')}}">Usuario</a></li>
                <li><a href="{{path('apunte_tipo_index')}}">Tipo Apunte</a></li>
            </ul>
        {% endblock %}
    </body>
</html>
